[phases.setup]
nixPkgs = ["nodejs", "pnpm"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm prisma generate", "pnpm prisma migrate deploy", "pnpm run build"]

[start]
cmd = "pnpm prisma migrate deploy && node dist/main"
